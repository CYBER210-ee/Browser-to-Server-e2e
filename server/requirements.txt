# server/requirements.txt
# Derived from server/main.py imports:
#   from fastapi import FastAPI, WebSocket, WebSocketDisconnect
#   from fastapi.responses import JSONResponse
#   (json + itertools.count are Python stdlib — no install needed)
#
# The WebSocket endpoint uses receive_json()/send_json(), which needs an ASGI
# server with WebSocket support. uvicorn[standard] provides the `websockets`
# library plus watchfiles for `--reload`.
#
# Install with:  pip install -r requirements.txt

fastapi==0.115.*
uvicorn[standard]==0.32.*
python-dotenv>=1.0
cryptography
pyhpke



# ADR 212 — sealed history records in Postgres (history_store.py)
psycopg[binary]>=3.2
